meta {
  name: Update viewing now status (ping to keep view current)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api_public/v1/products/:id/viewing-now
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {
    "viewerId": ""
  }
}

example {
  name: 200 Response
  description: Viewing status updated successfully
  
  request: {
    url: {{baseUrl}}/api_public/v1/products/:id/viewing-now
    method: POST
    mode: json
    params:path: {
      id: 
    }
  
    body:json: {
      {
        "viewerId": ""
      }
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
