meta {
  name: Link CMS content to user
  type: http
  seq: 17
}

post {
  url: {{baseUrl}}/api_admin/v1/cms/content/:id/user/:userId
  body: none
  auth: inherit
}

params:path {
  userId: 
}

example {
  name: 201 Response
  
  request: {
    url: {{baseUrl}}/api_admin/v1/cms/content/:id/user/:userId
    method: POST
    mode: none
    params:path: {
      userId: 
    }
  }
  
  response: {
    status: {
      code: 201
      text: Created
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
