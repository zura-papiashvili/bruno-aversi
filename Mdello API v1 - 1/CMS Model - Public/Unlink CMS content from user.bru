meta {
  name: Unlink CMS content from user
  type: http
  seq: 18
}

delete {
  url: {{baseUrl}}/api_admin/v1/cms/content/:id/user/:userId
  body: none
  auth: inherit
}

params:path {
  userId: 
}

example {
  name: 200 Response
  
  request: {
    url: {{baseUrl}}/api_admin/v1/cms/content/:id/user/:userId
    method: DELETE
    mode: none
    params:path: {
      userId: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
