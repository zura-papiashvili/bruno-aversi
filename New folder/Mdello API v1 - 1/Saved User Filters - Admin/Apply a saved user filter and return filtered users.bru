meta {
  name: Apply a saved user filter and return filtered users
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api_admin/v1/saved-user-filters/users/:id/apply
  body: none
  auth: inherit
}

params:path {
  id: 
}

example {
  name: 201 Response
  
  request: {
    url: {{baseUrl}}/api_admin/v1/saved-user-filters/users/:id/apply
    method: POST
    mode: none
    params:path: {
      id: 
    }
  }
  
  response: {
    status: {
      code: 201
      text: Created
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
