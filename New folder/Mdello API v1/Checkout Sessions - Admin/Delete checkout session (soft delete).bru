meta {
  name: Delete checkout session (soft delete)
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api_admin/v1/checkout-sessions/:id
  body: none
  auth: inherit
}

params:path {
  id: 
}

example {
  name: 200 Response
  
  request: {
    url: {{baseUrl}}/api_admin/v1/checkout-sessions/:id
    method: DELETE
    mode: none
    params:path: {
      id: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
