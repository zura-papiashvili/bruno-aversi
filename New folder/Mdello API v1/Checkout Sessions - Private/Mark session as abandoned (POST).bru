meta {
  name: Mark session as abandoned (POST)
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/checkout-sessions/:token/abandon
  body: none
  auth: inherit
}

params:path {
  token: 
}

example {
  name: 200 Response
  
  request: {
    url: {{baseUrl}}/api/v1/checkout-sessions/:token/abandon
    method: POST
    mode: none
    params:path: {
      token: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
