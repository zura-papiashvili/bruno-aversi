meta {
  name: Backfill product images from ERP IDs (Aversi)
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/api_admin/v1/products/sync-images-from-erp
  body: none
  auth: inherit
}

params:query {
  limit: 
}

example {
  name: 201 Response
  
  request: {
    url: {{baseUrl}}/api_admin/v1/products/sync-images-from-erp
    method: POST
    mode: none
    params:query: {
      limit: 
    }
  }
  
  response: {
    status: {
      code: 201
      text: Created
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
